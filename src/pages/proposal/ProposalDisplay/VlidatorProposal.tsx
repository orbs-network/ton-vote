import { Typography } from "@mui/material";
import { TitleContainer } from "components";

export const Cycles = () => {
  return (
    <TitleContainer title="Cycles">
      <Typography>test</Typography>
    </TitleContainer>
  );
};
