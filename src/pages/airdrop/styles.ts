import { styled } from "@mui/material";
import { Button, VirtualList } from "components";

export const StyledButton = styled(Button)({
  minWidth: 200,
});


export const StyledAirdropList = styled(VirtualList)({
  height: 400,
 
});